[project]
name = "secoda-analysis-mcp"
version = "0.3.3"
description = "Read-only MCP server for exploring and analysing a Secoda data catalog"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.6.0",
    "requests>=2.28.0",
    "pydantic>=2.0.0",
]
